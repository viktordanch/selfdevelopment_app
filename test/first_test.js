'use strict';
var chai = require('chai'),
    expect = chai.expect;

describe('Init Travis test', function () {
    it('always true', function() {
        return expect(true).to.eq(true);
    });
});
